#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>

/* prototype of functin _putchar */
int _putchar(char c);

/* prototype of function _puts_recursion */
void _puts_recursion(char *s);

/* prototype of function _print_rev_recursion */
void _print_rev_recursion(char *s);

/* prototype of function _strlen_recursion */
int _strlen_recursion(char *s);

/* prototype of function factorial */
int factorial(int n);

/* protoype of function _pow_recursion */
int _pow_recursion(int x, int y);

#endif /* MAIN_H */
